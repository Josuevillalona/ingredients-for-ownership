/**
 * DISABLED - Seed Foods functionality
 * This file is disabled as part of the clean slate approach for global food database
 * Foods will now be saved from the FDC API without pre-assigned colors
 */

// DISABLED: Seeding functionality removed for clean slate approach
export async function seedStandardFoods() {
  console.log("Food seeding disabled - using clean slate approach");
  return 0;
}

export async function initializeCoachFoodDatabase(coachId: string) {
  console.log("Coach food database initialization disabled - using clean slate approach");
  return 0;
}
